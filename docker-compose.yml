# Compose 使用仓库根目录 .env 作为全量配置来源。
# 标准启动命令：docker compose --env-file .env up -d --build

x-service-env: &service_env
  env_file:
    - .env

x-backend-overrides: &backend_overrides
  # 容器内服务地址覆盖本机 .env 地址；其余全量变量继续来自 env_file。
  DATABASE_URL: postgresql://${POSTGRES_USER:?set POSTGRES_USER in .env}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}@postgres:5432/${POSTGRES_DB:?set POSTGRES_DB in .env}
  REDIS_URL: redis://redis:6379/0
  AUTO_CREATE_TABLES: "false"
  RUNTIME_DATA_DIR: /app/data
  ARTIFACT_STORAGE_DIR: /app/data/artifacts
  STATIC_STORAGE_DIR: /app/data/static

services:
  # PostgreSQL 数据库
  postgres:
    image: pgvector/pgvector:pg16
    container_name: agent_interview_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?set POSTGRES_USER in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:?set POSTGRES_DB in .env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:${POSTGRES_PORT}:5432"
    networks:
      - agent_interview_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: agent_interview_redis
    restart: always
    ports:
      - "127.0.0.1:${REDIS_PORT}:6379"
    networks:
      - agent_interview_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 在 API 和 Worker 启动前执行可重复的 schema 初始化/增量迁移。
  migrate:
    <<: *service_env
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: agent_interview_migrate
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      <<: *backend_overrides
    volumes:
      - backend_runtime_data:/app/data
      - artifact_data:/app/data/artifacts
    command: ["python", "-m", "scripts.deployment", "migrate"]
    networks:
      - agent_interview_network
    healthcheck:
      disable: true

  # 后端 API
  backend:
    <<: *service_env
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: agent_interview_backend
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    environment:
      <<: *backend_overrides
    volumes:
      - backend_runtime_data:/app/data
      - artifact_data:/app/data/artifacts
    networks:
      - agent_interview_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "python -m scripts.deployment readiness && curl -fsS http://localhost:8000/health > /dev/null"]
      interval: 30s
      timeout: 10s
      retries: 3

  worker:
    <<: *service_env
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: agent_interview_worker
    restart: on-failure
    # 停止窗口：必须大于 AGENT_RUN_WORKER_SHUTDOWN_TIMEOUT_MS（默认 660000ms），
    # 为 Dramatiq 优雅停止预留额外进程清理缓冲；提高任务预算时同步检查两处。
    stop_grace_period: 720s
    command: ["/app/scripts/run-worker.sh"]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    environment:
      <<: *backend_overrides
    volumes:
      - backend_runtime_data:/app/data
      - artifact_data:/app/data/artifacts
    networks:
      - agent_interview_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "python -m scripts.deployment readiness"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 前端
  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:?set NEXT_PUBLIC_API_URL in .env}
    container_name: agent_interview_frontend
    restart: always
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - agent_interview_network

  # Nginx 反向代理
  # 无登录本地部署：只绑定本机 loopback，禁止对局域网/公网暴露。
  nginx:
    image: nginx:alpine
    container_name: agent_interview_nginx
    restart: always
    environment:
      TZ: Asia/Shanghai
    ports:
      - "127.0.0.1:${NGINX_PORT:-80}:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/logs:/var/log/nginx
      - /etc/localtime:/etc/localtime:ro
    depends_on:
      frontend:
        condition: service_started
      backend:
        condition: service_healthy
    networks:
      - agent_interview_network

volumes:
  postgres_data:
  backend_runtime_data:
  artifact_data:

networks:
  agent_interview_network:
    driver: bridge
