# 03｜前端与事件契约

来源：`docs/架构演进待办.md` 的未完成项。

推进重点：RunEvent / SSE 契约、前端 reducer、模块拆分、状态统一。

## 未完成项

- [x] AgentRun 或 RunEvent 保存 Prompt 版本；
  - [x] `run.created` 事件 payload 已带 `prompt_name` / `prompt_version`。
- [x] 区分运行事件、领域事件、UI delta 和审计事件；
  - [x] 已补统一 SSE 事件解析器，覆盖 `plan` / `step_update` / `agent_run_event` / `text` / `audio` / `progress` / `complete` / `done` / `error` / `state_update` / `run`；
  - [x] 已补 `audit` 事件和 `getStreamEventCategory` 分类：运行事件 / 领域事件 / UI delta / 审计事件。
- [x] 对每类事件增加 TypeScript 判别联合类型；
- [x] 增加断线、半包、粘包和未知版本测试。
- [x] 增加本地运行统计页面；
  - [x] 新增 `/runs` 页面，基于最近 100 条 `AgentRun` 本地聚合状态、任务类型和耗时。
- [x] 将 ResumeTools 按分析、优化、生成、素材拆分；
  - [x] 已抽出输入面板、会话选择器和空状态组件；
  - [x] 分析/优化/JD 结果面板已拆为 `ResumeResultPanels`。
- [x] SettingsDialog 按模型池、Voice、专家通道拆分；
  - [x] `ModelFormDialog` 已移出主文件；
  - [x] `ModelAssignments` 已承载模型池、简历专家通道和 Voice 通道配置。
- [x] VoiceInterview 抽出 session、SSE、音频播放 hooks；
  - [x] 已抽出 `useVoiceSseStream`，统一语音 SSE 读取与事件分发；
  - [x] session 初始化 hook；
  - [x] 音频播放 hook。
- [x] API 文件按资源拆分；
  - [x] AgentRun 类型、运行操作、事件订阅已拆为 `agentRunTypes.ts` / `agentRuns.ts` / `agentRunEvents.ts`；
  - [x] Resume API 类型已拆为 `resumeTypes.ts` 并保留兼容 re-export；
  - [x] Resume 素材、组装、项目重写 API 已拆为 `resumeMaterials.ts` 并保留兼容 re-export。
- [x] 减少组件直接调用 `useInterviewStore.getState()`；
  - [x] `VoiceInterview` 已移除直接 `getState()` 调用；
  - [x] 组件层剩余直接 `getState()` 已收敛到 `interviewFacade`。
- [x] 为跨 Slice 调用建立 selector/action facade；
  - [x] 新增 `web/store/interviewFacade.ts`，集中语音面试、API 配置、生成简历刷新等 store 快照和 action 调用。
- [x] 为 SSE parser 和 RunEvent reducer 增加单元测试；
- [x] 统一 RunEvent 信封；
- [x] 前端大型模块拆分；
  - [x] `VoiceInterview` hook 化；
  - [x] `ResumeTools` 抽出 `resume-tools/*` 子组件；
  - [x] `SettingsDialog` 抽出 `settings/*` 子组件；
- [x] 事件 reducer 与 UI 状态统一。
  - [x] `buildInteractiveExecutionPlan` 已从 `InterviewSlice` 移入 `agentRunEvents` 并补测试；
  - [x] `plan` / `step_update` / `agent_run_event` 执行计划更新已统一到 `reduceExecutionPlanStreamEvent`。
