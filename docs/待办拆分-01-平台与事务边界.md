# 01｜平台与事务边界

来源：`docs/架构演进待办.md` 的未完成项。

推进重点：基础设施、Application Use Case、UnitOfWork、Outbox、AgentRun 事务边界、仓库保存链路。


## 完成项

- [x] 使用真实 PostgreSQL/Redis 完成基础设施集成测试。
  - [x] 已建立 PostgreSQL AgentRun claim、PostgreSQL checkpoint、Redis WATCH/MULTI、Redis 模型池、Redis/Dramatiq Broker 集成测试；无外部 DSN 时安全 skip。
- [x] 新增 `backend/app/application/`；
- [x] API 逐步迁移到 Application Use Case；
  - [x] 会话管理 API 迁入 `app.application.interview.sessions`。
  - [x] 面经题目导入 API 迁入 `app.application.interview.experience_imports`。
  - [x] 面经采集 collect API 迁入 `app.application.interview.experience_imports`。
  - [x] 题库 CRUD / 检索 / 导入 / 会话沉淀 API 迁入 `app.application.question_bank`。
  - [x] 语音面试 start/clone API 迁入 `app.application.interview.voice`。
- [x] 引入轻量 Unit of Work；
  - [x] `UnitOfWork` 已提供统一 commit/rollback/close 边界。
- [x] 将跨领域流程从 API 和 AgentRun Executor 中逐步下沉到 Use Case。
  - [x] 投递追踪 API 走 `ApplicationUseCases`；投递事件状态变化接入 `UnitOfWork`。
  - [x] AgentRun Executor 支持延迟业务结果写入，由完成态事务统一提交。
- [x] 区分网络失败、Provider 失败和输出契约失败。
  - [x] 新增统一错误分类工具，覆盖网络、Provider、限流、认证、配额和输出契约失败。
- [x] 建立真实 PostgreSQL/Redis/Dramatiq 集成测试；
  - [x] 集成测试通过 `TEST_POSTGRES_DSN`、`TEST_REDIS_URL` 显式开启，避免误连真实环境。
- [x] 在临时旧 head 快照库上验证 Alembic 从旧 head 升级到最新 head。
  - [x] 新增旧 head `20260716_05` 到 latest head 的真实 PostgreSQL 升级验证；需 `TEST_POSTGRES_ALLOW_RESET=1`。
- [x] 禁止业务代码直接创建 `ChatOpenAI/AsyncOpenAI`；
  - [x] 增加模型客户端创建 allowlist 测试，仅允许 `app/services/llms.py` 作为统一工厂。
- [x] 区分网络失败、Provider 失败、限流和输出契约失败；
  - [x] 面试启动错误分类已复用统一分类器。
- [x] 优先覆盖 AgentRun 完成与业务结果落库；
  - [x] 简历优化 AgentRun 支持业务结果延迟落库，并与完成态共用同一 `UnitOfWork`。
- [x] 覆盖岗位资产、简历优化和投递状态变化；
  - [x] 简历优化结果、岗位资产状态、投递事件状态变化均支持外层 `UnitOfWork` 统一提交。
- [x] 工具声明 `read/write/external` effect；
  - [x] 新增 `ToolContract`，岗位/简历/面试/记忆工具均声明 effect。
- [x] 工具声明权限；
  - [x] 工具 metadata 持久携带权限标识，测试覆盖非空权限。
- [x] 写入和外部工具声明幂等键策略；
  - [x] `save_job`、`generate_assets` 声明幂等键策略。
- [x] 持久化 `tool.started/completed/failed`；
  - [x] InterviewRuntime trace 增加 `event_type=tool.started/tool.completed/tool.failed`、耗时和摘要字段。
- [x] 工具结果只保存必要摘要，避免敏感数据重复落库；
  - [x] 工具 trace 仅保存输入 200 字、输出 300 字摘要；工具契约声明 `result_retention`。
- [x] 明确数据库与 checkpoint 的事实来源优先级；
  - [x] `FACT_SOURCE_PRIORITY=(database, checkpoint)`，业务结果和 AgentRun 状态以数据库为准。
- [x] 定义长期记忆有效期和删除同步；
  - [x] 新增 `MEM0_RETENTION_DAYS`、`expires_at`、`delete_sync_policy` 元数据。
- [x] 保存重试原因；
  - [x] Outbox 新增 `last_error_type`、`last_failure_reason`。
- [x] 保存工具调用次数和耗时；
  - [x] Runtime 已维护 `tool_round_count`；tool trace 和 Outbox 均记录耗时。
- [x] 将首页模式切换与业务区域分离；
  - [x] 新增 `web/lib/navigation.ts`，公共主视图和需要 API 配置的业务视图分离。
- [x] 真实 PostgreSQL/Redis 集成测试；
  - [x] PostgreSQL/Redis 相关测试均使用显式环境变量开启，本地无外部服务时 skip。
- [x] 交互式 AgentRun；
  - [x] 已有 `interview_turn` / `voice_interview_turn` AgentRun 类型、事件流和取消恢复生命周期测试。
- [x] Application Use Case；
  - [x] 会话、面经、题库、语音、投递追踪等 API 已迁入应用层；边界测试覆盖。
- [x] Unit of Work；
  - [x] AgentRun 完成态、业务结果、岗位/投递状态变化接入 `UnitOfWork`。
- [x] Outbox；
  - [x] 已新增 `task_outbox` 表、投递扫描和 AgentRun enqueue 补偿重试。
- [x] API→Repository allowlist 逐步清零。
  - [x] `ALLOWED_API_REPOSITORY_IMPORTS` 为空，测试覆盖 API 不直接导入 Repository。
  - [x] `api/sessions.py` 不再直接导入 Repository，并补边界测试。
  - [x] `api/interview_experience.py` 的题目导入和采集不再直接导入 Repository/Service，并补边界测试。
  - [x] `api/question_bank.py` 不再直接导入 Repository，并从 allowlist 移除。
  - [x] `api/voice_chat.py` 不再直接导入 Repository，并补应用层委托测试。
  - [x] API→Repository allowlist 已清零。

