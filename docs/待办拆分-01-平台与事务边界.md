# 01｜平台与事务边界

来源：`docs/架构演进待办.md` 的未完成项。

推进重点：基础设施、Application Use Case、UnitOfWork、Outbox、AgentRun 事务边界、仓库保存链路。


## 未完成项

- [ ] 使用真实 PostgreSQL/Redis 完成基础设施集成测试。
- [ ] 新增 `backend/app/application/`；
- [ ] API 逐步迁移到 Application Use Case；
  - [x] 会话管理 API 迁入 `app.application.interview.sessions`。
  - [x] 面经题目导入 API 迁入 `app.application.interview.experience_imports`。
  - [x] 题库 CRUD / 检索 / 导入 / 会话沉淀 API 迁入 `app.application.question_bank`。
- [ ] 引入轻量 Unit of Work；
- [ ] 将跨领域流程从 API 和 AgentRun Executor 中逐步下沉到 Use Case。
- [ ] 区分网络失败、Provider 失败和输出契约失败。
- [ ] 建立真实 PostgreSQL/Redis/Dramatiq 集成测试；
- [ ] 在临时旧 head 快照库上验证 Alembic 从旧 head 升级到最新 head。
- [ ] 禁止业务代码直接创建 `ChatOpenAI/AsyncOpenAI`；
- [ ] 区分网络失败、Provider 失败、限流和输出契约失败；
- [ ] 优先覆盖 AgentRun 完成与业务结果落库；
- [ ] 覆盖岗位资产、简历优化和投递状态变化；
- [ ] 工具声明 `read/write/external` effect；
- [ ] 工具声明权限；
- [ ] 写入和外部工具声明幂等键策略；
- [ ] 持久化 `tool.started/completed/failed`；
- [ ] 工具结果只保存必要摘要，避免敏感数据重复落库；
- [ ] 明确数据库与 checkpoint 的事实来源优先级；
- [ ] 定义长期记忆有效期和删除同步；
- [ ] 保存重试原因；
- [ ] 保存工具调用次数和耗时；
- [ ] 将首页模式切换与业务区域分离；
- [ ] 真实 PostgreSQL/Redis 集成测试；
- [ ] 交互式 AgentRun；
- [ ] Application Use Case；
- [ ] Unit of Work；
- [ ] Outbox；
- [ ] API→Repository allowlist 逐步清零。
  - [x] `api/sessions.py` 不再直接导入 Repository，并补边界测试。
  - [x] `api/interview_experience.py` 的题目导入不再直接导入 Repository，并补边界测试。
  - [x] `api/question_bank.py` 不再直接导入 Repository，并从 allowlist 移除。

